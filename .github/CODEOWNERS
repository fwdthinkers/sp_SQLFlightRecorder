# CODEOWNERS — sp_SQLFlightRecorder
# Per D-163 (route by area) and D-185 as amended by D-191.
#
# The owner below is the repository owner account, @forward-thinkers-lab. This
# repository is owned by a user account, not an organization, so GitHub team
# syntax (@org/team) cannot resolve here — a user account has no teams. The
# owner account is a valid CODEOWNERS owner and these rules take effect as
# written.
#
# D-185 originally called for a single @core-maintainers team at v1.0 launch.
# That presumed an organization. If the project later moves under one, change
# the owners below to a team such as @org/core-maintainers, give that team write
# access to the repository, and split by area as sketched at the foot of this
# file (D-185's topic-team intent, preserved by D-191).
#
# Note that a repository owner's own pull requests are not review-blocked by
# CODEOWNERS — GitHub does not request a review from the PR author. The
# two-reviewer rule for the artifact (D-158) therefore depends on a second
# human reviewer being available, not on this file alone.

# Default owner for everything.
*                                   @forward-thinkers-lab

# The shipped artifact requires the two-reviewer rule (D-158); routing it
# explicitly keeps the safety-critical file visible to the owner.
/sp_SQLFlightRecorder.sql           @forward-thinkers-lab

# Safety-critical build-pipeline guardrails.
/tests/static-analysis/             @forward-thinkers-lab
/.github/workflows/                 @forward-thinkers-lab

# If the project moves to an organization with topic teams (D-185/D-191), split
# e.g.:
#   /docs/rules/          @org/rule-pack-maintainers
#   /docs/compatibility/  @org/compat-maintainers
